sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t vw312/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true vw312/react-test npm test

after_success:
  - docker build -t vw312/multi-client ./client
  - docker build -t vw312/multi-nginx ./nginx
  - docker build -t vw312/multi-server ./server
  - docker build -t vw312/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push vw312/multi-client
  - docker push vw312/multi-nginx
  - docker push vw312/multi-server
  - docker push vw312/multi-worker
